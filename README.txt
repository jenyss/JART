### Art Gallery Project

npm install #Install dependencies

rm -rf .next #Clean the Cache

npm install -D tailwindcss postcss autoprefixer #Tailwind CSS and its dependencies (postcss and autoprefixer) are installed correctly
npx tailwindcss init -p

npm run dev

#If something fails always try
rm -rf node_modules
npm install

#Tailwind Configuration: Verify Tailwind dependencies (tailwindcss, postcss, autoprefixer) and PostCSS setup with:
npx tailwindcss init -p



